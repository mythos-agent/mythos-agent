{
  "name": "mythos-agent-vscode",
  "displayName": "mythos-agent — AI Security Scanner",
  "description": "Find vulnerabilities, chain them into attack paths, and auto-fix with AI. Mythos for everyone.",
  "version": "0.3.0",
  "publisher": "mythos-agent",
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Linters",
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "security",
    "vulnerability",
    "scanner",
    "ai",
    "owasp",
    "sast"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mythos-agent.scanFile",
        "title": "mythos-agent: Scan Current File"
      },
      {
        "command": "mythos-agent.scanWorkspace",
        "title": "mythos-agent: Scan Workspace"
      },
      {
        "command": "mythos-agent.fixVulnerability",
        "title": "mythos-agent: Fix Vulnerability with AI"
      },
      {
        "command": "mythos-agent.clearDiagnostics",
        "title": "mythos-agent: Clear All Diagnostics"
      }
    ],
    "configuration": {
      "title": "mythos-agent",
      "properties": {
        "mythos-agent.scanOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Automatically scan files for vulnerabilities on save"
        },
        "mythos-agent.severity": {
          "type": "string",
          "default": "low",
          "enum": ["critical", "high", "medium", "low"],
          "description": "Minimum severity level to display"
        },
        "mythos-agent.enableSecrets": {
          "type": "boolean",
          "default": true,
          "description": "Enable secrets detection"
        },
        "mythos-agent.enableAI": {
          "type": "boolean",
          "default": false,
          "description": "Enable AI-powered deep analysis (requires API key)"
        },
        "mythos-agent.apiKey": {
          "type": "string",
          "default": "",
          "description": "Anthropic API key for AI features"
        }
      }
    }
  },
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "watch": "npm run build -- --watch",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.24.0",
    "@vscode/vsce": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
